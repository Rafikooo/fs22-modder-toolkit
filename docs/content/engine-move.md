---
title: Engine Functions - Move
description: Move functions from FS22 engine
---

# Move Functions

1 functions starting with `move`.

### `moveCCT()`

Enqueue character movement

**Parameters:**

- `characterIndex` (`integer`) - characterIndex
- `x` (`float`) - x
- `y` (`float`) - y
- `z` (`float`) - z
- `collisionMasks` (`integer`) - collisionMasks

```lua
moveCCT(characterIndex, x, y, z, collisionMasks)
```

