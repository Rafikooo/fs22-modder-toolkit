---
title: Engine Functions - Write
description: Write functions from FS22 engine
---

# Write Functions

1 functions starting with `write`.

### `writeFillPlaneToStream()`

Write fill plane surface information to stream

**Parameters:**

- `fillPlaneId` (`integer`) - fillPlaneId
- `streamId` (`integer`) - streamId
- `_` (`integer`)
- `_` (`integer`)
- `_` (`integer`)
- `_` (`integer`)
- `_` (`float`)
- `_` (`float`)
- `_` (`float`)

**Returns:** `boolean`

```lua
local result = writeFillPlaneToStream(fillPlaneId, streamId, _, _, _, _, _, _, _)
```

