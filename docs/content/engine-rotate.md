---
title: Engine Functions - Rotate
description: Rotate functions from FS22 engine
---

# Rotate Functions

1 functions starting with `rotate`.

### `rotateAboutLocalAxis()`

Rotate about local axis

**Parameters:**

- `transformId` (`integer`) - transformId
- `rotation` (`float`) - rotation
- `x` (`float`) - x
- `y` (`float`) - y
- `z` (`float`) - z
- `_` (`integer`)
- `_` (`string`)

**Returns:** `boolean`

```lua
local result = rotateAboutLocalAxis(transformId, rotation, x, y, z, _, _)
```

