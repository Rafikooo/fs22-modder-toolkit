---
title: Engine Functions - Invalidate
description: Invalidate functions from FS22 engine
---

# Invalidate Functions

1 functions starting with `invalidate`.

### `invalidateShapeBoundingVolume()`

This needs to be called after changing the bounding volume of the geometry if the bounding volume of the shape is not invalidated otherwise

**Parameters:**

- `shapeId` (`integer`)

```lua
invalidateShapeBoundingVolume(shapeId)
```

