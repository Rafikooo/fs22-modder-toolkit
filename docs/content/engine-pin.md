---
title: Engine Functions - Pin
description: Pin functions from FS22 engine
---

# Pin Functions

1 functions starting with `pin`.

### `pinSharedI3DFileInCache()`

Pin a shared I3D file into the cache, so it's not auto-deleted

**Parameters:**

- `filename` (`string`) - filename
- `verbose` (`boolean`) - verbose

```lua
pinSharedI3DFileInCache(filename, verbose)
```

