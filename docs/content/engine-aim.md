---
title: Engine Functions - Aim
description: Aim functions from FS22 engine
---

# Aim Functions

1 functions starting with `aim`.

### `aimCamera()`

Aim camera

**Parameters:**

- `cameraId` (`integer`) - cameraId
- `x` (`float`) - x
- `y` (`float`) - y
- `z` (`float`) - z
- `distance` (`float`) - distance
- `dt` (`float`) - dt
- `springStrength` (`float`) - springStrength

```lua
aimCamera(cameraId, x, y, z, distance, dt, springStrength)
```

