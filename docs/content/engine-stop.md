---
title: Engine Functions - Stop
description: Stop functions from FS22 engine
---

# Stop Functions

2 functions starting with `stop`.

### `stopSample()`

Stop sample object

**Parameters:**

- `sampleId` (`integer`) - sampleId
- `delay` (`float`) - delay until to stop playing
- `fadeTime` (`float`) - time to fade out the sample

```lua
stopSample(sampleId, delay, fadeTime)
```

### `stopStreamedSample()`

Stop streamed sample

**Parameters:**

- `sampleId` (`integer`) - sampleId

```lua
stopStreamedSample(sampleId)
```

