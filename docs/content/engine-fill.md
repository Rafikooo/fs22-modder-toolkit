---
title: Engine Functions - Fill
description: Fill functions from FS22 engine
---

# Fill Functions

1 functions starting with `fill`.

### `fillPlaneAdd()`

Add material/volume to a fill plane

**Parameters:**

- `fillPlaneId` (`integer`) - fillPlaneId
- `dTvolume` (`float`) - dTvolume
- `x` (`float`) - x
- `y` (`float`) - y
- `z` (`float`) - z
- `dx1` (`float`) - dx1
- `dy1` (`float`) - dy1
- `dz1` (`float`) - dz1
- `dx2` (`float`) - dx2
- `dy2` (`float`) - dy2
- `dz2` (`float`) - dz2

```lua
fillPlaneAdd(fillPlaneId, dTvolume, x, y, z, dx1, dy1, dz1, dx2, dy2, dz2)
```

