---
title: Engine Functions - Assign
description: Assign functions from FS22 engine
---

# Assign Functions

1 functions starting with `assign`.

### `assignAnimTrackClip()`

Assign clip to animation track

**Parameters:**

- `characterSetId` (`integer`) - characterSetId
- `trackId` (`integer`) - trackId
- `clipIndex` (`integer`) - clipIndex

```lua
assignAnimTrackClip(characterSetId, trackId, clipIndex)
```

