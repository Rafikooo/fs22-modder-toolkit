---
title: Engine Functions - Update
description: Update functions from FS22 engine
---

# Update Functions

2 functions starting with `update`.

### `updateConditionalAnimation()`

Update

**Parameters:**

- `conditionalAnimationId` (`integer`) - conditionalAnimationId
- `dt` (`float`) - dt

```lua
updateConditionalAnimation(conditionalAnimationId, dt)
```

### `updateDifferential()`

update differential

**Parameters:**

- `transformId` (`integer`) - transformId
- `index` (`integer`) - index
- `ratio` (`float`) - ratio
- `bias` (`float`) - bias
- `_` (`integer`)

```lua
updateDifferential(transformId, index, ratio, bias, _)
```

