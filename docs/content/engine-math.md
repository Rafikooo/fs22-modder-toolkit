---
title: Engine Functions - Math
description: Math functions from FS22 engine
---

# Math Functions

1 functions starting with `math`.

### `mathEulerToQuaternion()`

Euler angle to quaternion

**Parameters:**

- `x` (`float`) - x
- `y` (`float`) - y
- `z` (`float`) - z
- `_` (`float`)
- `_` (`float`)
- `_` (`float`)
- `_` (`float`)
- `_` (`float`)
- `_` (`float`)
- `_` (`float`)

**Returns:** `float`

```lua
local result = mathEulerToQuaternion(x, y, z, _, _, _, _, _, _, _)
```

