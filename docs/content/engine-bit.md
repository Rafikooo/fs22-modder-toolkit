---
title: Engine Functions - Bit
description: Bit functions from FS22 engine
---

# Bit Functions

4 functions starting with `bit`.

### `bitAND()`

bit AND-Operation

**Parameters:**

- `value1` (`integer`) - value1
- `value2` (`integer`) - value2

**Returns:** `integer`

```lua
local result = bitAND(value1, value2)
```

### `bitHighestSet()`

bit HighestSet-Operation

**Parameters:**

- `input` (`integer`) - input

**Returns:** `integer`

```lua
local result = bitHighestSet(input)
```

### `bitOR()`

bit OR-Operation

**Parameters:**

- `value1` (`integer`) - value1
- `value2` (`integer`) - value2

**Returns:** `integer`

```lua
local result = bitOR(value1, value2)
```

### `bitShiftRight()`

bit ShiftRight-Operation

**Parameters:**

- `value1` (`integer`) - value1
- `value2` (`integer`) - value2

**Returns:** `integer`

```lua
local result = bitShiftRight(value1, value2)
```

