name: Deploy Documentation

on:
  push:
    branches:
      - main
    paths:
      - 'src/docs-generator/outputs/docs/**'
      - 'src/docs-generator/outputs/docusaurus.config.js'
      - 'src/docs-generator/outputs/sidebars.js'
      - 'src/docs-generator/outputs/package.json'
      - '.github/workflows/deploy-docs.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    name: Build Documentation Site
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          cd src/docs-generator/outputs
          npm install

      - name: Build static site
        run: |
          cd src/docs-generator/outputs
          npm run build

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'src/docs-generator/outputs/build'

  deploy:
    name: Deploy to GitHub Pages
    environment:
      name: github-pages
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
